# Committed instances

Three canonical instances used throughout the report, built from named
Dublin locations (not random points) so they are legible in the write-up:
`small` (N=8), `medium` (N=20), `large` (N=40). Added in Stage 2.
