# Results

Gitignored except this file. Every run writes to `results/<run-id>/`, with
its `config.yaml` and `result.json` alongside any maps/figures it produced,
so a run's output is self-describing and reproducible from its own config.

Populated starting Stage 4 (`dlm plan`) and Stage 6 (`dlm compare`).
