"""Dynamic delivery instances: depot + N user-chosen stops.

Built in Stage 2 (schema, builder, presets, geocoding) and Stage 3 (travel-time
matrix). ``N`` is always read from the instance object and is never a stored
constant anywhere downstream. See ``docs/stages/stage-02-instances.md``.
"""
