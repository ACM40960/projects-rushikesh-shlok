"""``Solution`` model and the ``Solver`` protocol every solver implements.

Stage 4 responsibility. All solvers share this interface so they drop into
the Stage 6 experiment harness unchanged, including the Stage 8 OR-Tools
benchmark solver.
"""
