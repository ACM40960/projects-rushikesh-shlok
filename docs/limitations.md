# Limitations

Consolidated in Stage 9 from the "Known limitations" section of every
individual stage doc. Expected to include: no turn restrictions, no signal
delay, no live traffic, imputed speeds where OSM `maxspeed` is absent,
static service times, single-day horizon. See
`docs/stages/stage-09-hardening.md`.
