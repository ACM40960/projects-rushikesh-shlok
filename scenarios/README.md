# Scenarios

Version-controlled disruption scenario YAML files, authored by users and by
the project's curated library. The schema is defined and documented here in
Stage 5 (see `docs/stages/stage-05-disruptions.md`), with a worked example.

This directory is empty until Stage 5.
